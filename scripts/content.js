const login = document.getElementById("login");
if (login) {
  var text = "";
  login.value = text; // UPDATE THIS PART WITH YOUR LOGIN EX; 15122
}

const password = document.getElementById("password");
if (password) {
  var text = ""; // UPDATE THIS PART WITH YOUR PASSWORD EX: 090605
  password.value = text;
}
