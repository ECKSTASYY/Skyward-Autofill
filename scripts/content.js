const login = document.getElementById("login");
if (login) {
  var text = "";
  login.value = text;
  console.log("done");
}

const password = document.getElementById("password");
if (password) {
  var text = "";
  password.value = text;
  console.log("done");
}
